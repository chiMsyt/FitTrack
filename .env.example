# FitTrack — Environment Configuration
# Copy this file to .env and fill in your MySQL credentials.
# NEVER commit .env to version control.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=fittrack_db
DB_USER=root
DB_PASSWORD=your_password_here

# Default user loaded on startup (matches seed data)
APP_USER_ID=1
